apiVersion: v1
kind: Service

metadata:
  name: cart-service-svc
  namespace: nexcart

spec:
  selector:
    app: cart-service

  ports:
    - protocol: TCP
      port: 3004
      targetPort: 3004

  type: ClusterIP
