apiVersion: v1
kind: Service

metadata:
  name: gateway-svc
  namespace: nexcart

spec:
  selector:
    app: gateway

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: ClusterIP
